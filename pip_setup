# Create a virtual environment in the project directory
cd ~/software/android-bundle-uploader
python3 -m venv env

# Activate it
source env/bin/activate

# Install the required packages
pip install google-api-python-client oauth2client httplib2

# Now run your script
python3 main.py ../generika_android/generika.json org.oddb.generika /home/zdavatz/software/generika_android/app/build/outputs/bundle/release/app-release.aab production
