#!/bin/bash
cd ~/software/android-bundle-uploader
python3 -m venv env
# Install packages using the venv's pip directly
env/bin/pip install google-api-python-client oauth2client httplib2
# Run using the venv's python directly
env/bin/python3 main.py ../generika_android/generika.json org.oddb.generika /home/zdavatz/software/generika_android/app/build/outputs/bundle/release/app-release.aab production
